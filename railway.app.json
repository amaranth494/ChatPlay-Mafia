{
  "$schema": "https://railway.app/schema.json",
  "name": "chatplay-backend",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd backend && npm install && npm run build",
    "startCommand": "cd backend && npm run start"
  },
  "run": {
    "port": 3001
  },
  "rootDirectory": "backend"
}